<%= simple_form_for [@pack, @card] do |f| %>
  <%= f.input :original_text %>
  <%= f.input :translated_text %>
  <%= f.input :picture, as: :file %>
  <% unless @card.new_record? %>
    <%= f.input :review_date %>
  <% end %>
  <%= f.submit %>
<% end %>
